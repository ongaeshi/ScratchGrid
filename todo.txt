*バグフィックス
Expectが非表示の場合は、値が入力されていても判定しない

x, yが使われていて、yが出力専用になっている際、x未入力部分が入力していないと反応してしまう

*新機能
外部ライブラリのインポート機能

書き換えを禁止する構文(セルのロック)

ファイル入出力
  セーブファイル(状態の保存)
  csv出力、csvインポート

外部エディタとの連携

ソースコードへの色づけ

表の行数を追加する処理
  今の所困った時無いけど、必要だろうなあ

簡単なテキストレベルでのエラーチェック
  D.evalがたまにパースにこけて返ってこなくなる時があるので、それらを防ぎたい。
  たとえば行頭に'/'だけとか。

予約語を使おうとしたら警告する

他の言語でも表示出来るようにしたい
  とりあえずRubyが簡単そう
